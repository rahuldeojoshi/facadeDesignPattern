package command;

// import facade.*;

/**
 * @author RDJ
 *
 */
public class Redo implements Command {

//	private Command lastCommand;
	
	/* 
	 * @see command.Command#execute()
	 */
	@Override
	public void execute() {
		// try{
		// int index = FileSystem.getHistory().size() - 1;
		// lastCommand = FileSystem.getCommand().get(index);
		// lastCommand.execute();
		// }catch(Exception e){
		// 	System.out.println("Usage: redo");
		// }
	}

}
